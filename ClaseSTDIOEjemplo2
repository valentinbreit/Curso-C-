#include <iostream>

int main() {
    std::string nombre;
    int edad;
    double salario;
    
    std::cout << "Ingrese su nombre: ";
    std::cin >> nombre;
    
    std::cout << "Ingrese su edad: ";
    std::cin >> edad;
    
    std::cout << "Ingrese su salario: ";
    std::cin >> salario;
    
    std::cout << "Nombre: " << nombre << ", Edad: " << edad << " años, Salario: $" << salario << std::endl;
    return 0;
}
